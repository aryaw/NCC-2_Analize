# netlify.toml

[build]
  command = "docker build -t botnet-viewer . && docker run -d -p 5000:5000 botnet-viewer"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "http://localhost:5000/:splat"
  status = 200
  force = true
